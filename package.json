{
  "name": "gamesearch-proxy",
  "version": "1.0.0",
  "description": "proxy for gamesearch app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tom Hammersley",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^2.0.0",
    "morgan": "^1.10.0"
  }
}
